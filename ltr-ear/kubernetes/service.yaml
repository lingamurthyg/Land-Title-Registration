apiVersion: v1
kind: Service
metadata:
  name: ltr-ear-service
  namespace: ltr-ear
  labels:
    app: ltr-ear
    component: application
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: ltr-ear
